server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ticketsupport_tickets?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 0977
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

app.cors.allowed-origin: http://localhost:5173
app.user-service.base-url: http://localhost:8081
